{
  "name": "eb-options",
  "version": "1.0.0",
  "description": "Set elastic beanstalk options using secrets manager",
  "main": "env.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Zeal Murapa",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-elastic-beanstalk": "^3.600.0",
    "@aws-sdk/client-secrets-manager": "^3.600.0",
    "dotenv": "^16.4.5"
  },
  "peerDependencies": {
    "@aws-sdk/client-elastic-beanstalk": "^3.600.0",
    "@aws-sdk/client-secrets-manager": "^3.600.0",
    "dotenv": "^16.4.5"
  }
}
